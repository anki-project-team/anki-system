# System Architecture

Frontend → Backend → Database

Components:
- Frontend (UI)
- Backend (Logic + API)
- Database (Storage)

Flow:
1. User answers a card
2. Backend calculates next interval
3. Data is stored in database
4. Sync system updates other clients
